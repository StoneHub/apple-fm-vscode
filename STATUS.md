# Status

Local prototype reviewed by the parent Astra thread; ready for Monroe's visual trial.

- Commit: `0ee3c93`.
- Artifact: `apple-fm-inline-completion-0.1.2.vsix` (generated by `npm run package`).
- Install: `code --install-extension apple-fm-inline-completion-0.1.1.vsix`
- Remove: `code --uninstall-extension local.apple-fm-inline-completion`
- Try: open a local or untitled file, invoke `Apple FM: Request Suggestion`, and accept through VS Code's normal inline completion control.
- Backend: CLI is default and invokes `/usr/bin/fm respond --model system --no-stream --greedy` with stdin. Swift is explicit and uses the packaged `bin/apple-fm-helper` unless a user-level path overrides it.

Checks run:

- `npm install --no-audit --no-fund`
- `npm run compile`
- `npm run package`
- Live request on macOS 27 build 26A428 using `/usr/bin/fm` returned a real response. The tested model returned a fenced, multi-line answer; the adapter removes fences and rejects control characters, while the provider leaves insertion decisions to VS Code.
- Focused fixtures passed for one transport-newline removal with leading whitespace preserved, unsafe Swift insertion rejection, and cancellation. The source implements bounded nearby/current-file context, unsaved `TextDocument` reads, credential filename exclusion, 15-second timeout, stale document/version/cursor rejection, malformed helper output, and child cleanup.
- Regression fixtures pass for exact echoed greeting-prefix removal, indentation preservation, midline suffix removal, and unsafe/empty normalization. Automatic completion is suppressed after accepting the offered insertion on that line; explicit requests remain available.
- Status bar is clickable and opens a transient in-memory flyout with backend/context, latest serialized stdin, exact argv, input/output sizes, and actions for enable/disable and VS Code inline-provider settings. It retains only the latest request in memory.

Parent checks passed: TypeScript compile; exact leading/trailing insertion whitespace; rejection of unsafe Swift insertion; three concurrent requests return cancelled/cancelled/ok; missing executable leaves the runner usable; bundled Swift helper returned a real completion. These were executed against the compiled backend. The parent also reviewed provider context, cancellation, and configuration handling.

Not yet evidenced: visual VS Code ghost-text acceptance, untitled UI behavior, and interaction with another inline provider. Remote workspaces are deliberately unsupported. No persistent process is owned or left running.

User trial found VS Code Restricted Mode disabled 0.1.0. Version 0.1.1 declares Restricted Mode support: document content is data only, the CLI path is fixed, and the optional helper path is application-scoped. No workspace trust setting was changed.
