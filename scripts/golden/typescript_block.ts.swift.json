{
  "fixture": "typescript_block.ts",
  "backend": "swift",
  "raw": "  const sum = values.reduce((acc, val) => acc + val, 0);\n  return sum / values.length;",
  "insertion": "const sum = values.reduce((acc, val) => acc + val, 0);\n  return sum / values.length;",
  "verdict": "good"
}
